export type PrescriptionMedicationItem = {
  id: string;
  name: string;
  dosage: string;
  frequency: string;
  duration: string;
};

export type PrescriptionDraftForm = {
  name: string;
  dosage: string;
  frequency: string;
  duration: string;
};
