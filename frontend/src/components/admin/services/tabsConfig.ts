// This page is the service-provider directory (dynamic Service Types +
// Service Providers catalog). Type tabs are now built at runtime from
// `useServiceTypesList()` instead of a hardcoded facility-type list — see
// AdminServicesPage. This constant is the only thing left shared across the
// module's components.
export const ADMIN_SERVICES_PAGE_SIZE = 10;
